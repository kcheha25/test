# 🔹 Génération de l'histogramme du nombre de feuillets détectés
def plot_sheets_histogram(feuille_counts):
    plt.figure(figsize=(8, 6))
    plt.hist(feuille_counts, bins=np.arange(min(feuille_counts), max(feuille_counts) + 1), alpha=0.7, color='blue', edgecolor='black')
    plt.xlabel("Nombre de feuillets détectés par ROI")
    plt.ylabel("Fréquence")
    plt.title("Histogramme du nombre de feuillets détectés par ROI")
    plt.show()
