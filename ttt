                x_filtered = []
                y_filtered = []
                for x_val, y_val in zip(row['x'], row['y']):
                    if x_val < 150:
                        x_filtered.append(x_val)
                        y_filtered.append(y_val)