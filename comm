# Évaluation du modèle sur l'ensemble de test (X_test, Y_test)
# Cette méthode retourne les valeurs des métriques définies lors de la compilation du modèle (ex: loss, accuracy, AUC)
test_loss, test_accuracy, test_auc = model.evaluate(X_test, Y_test, verbose=1)

# Affichage de la perte (loss) calculée sur les données de test
print(f"Test Loss: {test_loss}")

# Affichage de la précision (accuracy) obtenue sur les données de test
print(f"Test Accuracy: {test_accuracy}")

# Affichage de l'AUC (Area Under Curve) sur les données de test
print(f"Test AUC: {test_auc}")
